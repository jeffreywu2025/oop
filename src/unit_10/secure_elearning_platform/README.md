# Secure E-learning Platform

## Run unit tests
From this folder:

```bash
python -m unittest -v
```

## Project layout
- `elearn/` - package with modules (layered approach: domain/service/infrastructure)
- `tests/` - unit tests (TDD)
